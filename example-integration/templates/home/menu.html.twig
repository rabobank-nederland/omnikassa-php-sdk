{% extends 'base.html.twig' %}
{% block title %}Menu{% endblock %}
{% block body %}
    <h1>SDK Example Menu</h1>
    <div class="menu-buttons">
        <a href="{{ path('order_status') }}" class="btn btn-warning">Order Status</a>
        <a href="{{ path('sample_checkout') }}" class="btn btn-success">Sample Checkout</a>
        <a href="{{ path('manual_checkout') }}" class="btn btn-info">Manual Checkout</a>
        <a href="{{ path('payment_brands') }}" class="btn btn-dark">Payment Brands</a>
        <a href="{{ path('ideal_issuers') }}" class="btn btn-secondary">Show iDEAL Issuers</a>
        <a href="{{ path('callback') }}" class="btn btn-warning">Callback</a>
        <a href="{{ path('stored_cards') }}" class="btn btn-dark">Stored Cards</a>
        <a href="{{ path('refund') }}" class="btn btn-warning">Refund</a>
    </div>
{% endblock %}
