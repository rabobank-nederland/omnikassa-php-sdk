<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="{{ asset('styles.css') }}">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <nav style="background-color: #f8f9fa; padding: 1em; margin-bottom: 2em; border-bottom: 1px solid #dee2e6;">
            <div style="max-width: 1200px; margin: 0 auto;">
                <h2 style="margin: 0 0 0.5em 0; color: #495057;">OmniKassa SDK Examples</h2>
                <div>
                    <a href="{{ path('home') }}" style="text-decoration: none; color: #007bff; margin-right: 1em; padding: 0.5em 1em; border: 1px solid #007bff; border-radius: 0.25em;">Home</a>
                    <a href="{{ path('sample_checkout') }}" style="text-decoration: none; color: #28a745; margin-right: 1em; padding: 0.5em 1em; border: 1px solid #28a745; border-radius: 0.25em;">Sample Checkout</a>
                    <a href="{{ path('manual_checkout') }}" style="text-decoration: none; color: #17a2b8; margin-right: 1em; padding: 0.5em 1em; border: 1px solid #17a2b8; border-radius: 0.25em;">Manual Checkout</a>
                    <a href="{{ path('payment_brands') }}" style="text-decoration: none; color: #17a2b8; margin-right: 1em; padding: 0.5em 1em; border: 1px solid #17a2b8; border-radius: 0.25em;">Payment Brands</a>
                    <a href="{{ path('ideal_issuers') }}" style="text-decoration: none; color: #6c757d; padding: 0.5em 1em; border: 1px solid #6c757d; border-radius: 0.25em;">iDEAL Issuers</a>
                </div>
            </div>
        </nav>
        {% block body %}{% endblock %}
    </body>
</html>
